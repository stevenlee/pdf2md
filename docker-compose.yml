services:
  pdf2md:
    build: .
    container_name: pdf2md-converter
    volumes:
      - ./pdf2mdVault:/app/pdf2mdVault
      - ./prompts:/app/prompts
      - ./src:/app/src
      - ./.cache/pdf2md/marker:/root/.cache/marker
      - ./.cache/pdf2md/huggingface:/root/.cache/huggingface
      - ./.cache/pdf2md/datalab:/root/.cache/datalab
    shm_size: '4gb'  # 增加共享記憶體，防止 PyTorch 崩潰
    env_file:
      - .env
    environment:
      - TORCH_DEVICE=cuda
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_HOME=/root/.cache/huggingface
      - MARKER_DATA_DIR=/root/.cache/marker
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: "no"
